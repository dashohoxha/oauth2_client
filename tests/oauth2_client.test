<?php

/**
 * @file
 * OAuth2 Client tests.
 */

/**
 * Test OAuth2 Client.
 */
class OAuth2ClientTestCase extends DrupalWebTestCase {
  protected $profile = 'testing';

  public static function getInfo() {
    return array(
      'name' => 'OAuth2 Client',
      'description' => 'Tests basic OAuth2 Client functionality.',
      'group' => 'OAuth2',
    );
  }

  public function setUp() {
    parent::setUp('oauth2_client_test');
  }

  /**
   * Tests the client-credentials flow.
   */
  public function testClientCredentialsFlow() {
    $result = $this->drupalGet('oauth2/test/client-credentials');
    debug($result);
  }
}
